RSpec.describe Firebase::Auth::IdTokenKeeper do
  it "has a version number" do
    expect(Firebase::Auth::IdTokenKeeper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
